# OUxInfo
OUxInfo is a C++ based fast Shannon entropy estimator for Python.

## Dependencies
* Python
* gcc

## Setup
1. Install Boost
~~~bash
$ sudo apt install libboost-dev
~~~

2. Compile OUxInfo
~~~bash
$ pip install .
~~~

